
  /// % RANKING
  internal static func ranking() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "% RANKING")
    } else {
      return L10n.tr_en_us("LiteralAdded", "% RANKING")
    }
  }
  /// ADD ASSET TO STORE
  internal static func addAssetToStore() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "ADD ASSET TO STORE")
    } else {
      return L10n.tr_en_us("LiteralAdded", "ADD ASSET TO STORE")
    }
  }
  /// ADD EQUIPMENT
  internal static func addEquipment() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "ADD EQUIPMENT")
    } else {
      return L10n.tr_en_us("LiteralAdded", "ADD EQUIPMENT")
    }
  }
  /// Added selected photos successfully
  internal static func addedSelectedPhotosSuccessfully() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Added selected photos successfully")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Added selected photos successfully")
    }
  }
  /// ADDJUST VEHICLE STOCK
  internal static func addjustVehicleStock() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "ADDJUST VEHICLE STOCK")
    } else {
      return L10n.tr_en_us("LiteralAdded", "ADDJUST VEHICLE STOCK")
    }
  }
  /// ADJUST
  internal static func adjust() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "ADJUST")
    } else {
      return L10n.tr_en_us("LiteralAdded", "ADJUST")
    }
  }
  /// Amount
  internal static func amount() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Amount")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Amount")
    }
  }
  /// Back
  internal static func back() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Back")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Back")
    }
  }
  /// Balance Collections
  internal static func balanceCollections() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Balance Collections")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Balance Collections")
    }
  }
  /// Case Price
  internal static func casePrice() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Case Price")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Case Price")
    }
  }
  /// cases
  internal static func cases() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "cases")
    } else {
      return L10n.tr_en_us("LiteralAdded", "cases")
    }
  }
  /// CHATS
  internal static func chats() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "CHATS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "CHATS")
    }
  }
  /// Close
  internal static func close() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Close")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Close")
    }
  }
  /// COLLECTIONS BALANCING
  internal static func collectionsBalancing() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "COLLECTIONS BALANCING")
    } else {
      return L10n.tr_en_us("LiteralAdded", "COLLECTIONS BALANCING")
    }
  }
  /// COMPANY PIN
  internal static func companyPin() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "COMPANY PIN")
    } else {
      return L10n.tr_en_us("LiteralAdded", "COMPANY PIN")
    }
  }
  /// Complete Visit
  internal static func completeVisit() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Complete Visit")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Complete Visit")
    }
  }
  /// CONTACTS
  internal static func contacts() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "CONTACTS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "CONTACTS")
    }
  }
  /// COUNT VEHICLE STOCK
  internal static func countVehicleStock() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "COUNT VEHICLE STOCK")
    } else {
      return L10n.tr_en_us("LiteralAdded", "COUNT VEHICLE STOCK")
    }
  }
  /// CREATE NEW OBJECTIVE FOR SOMEONE ELSE
  internal static func createNewObjectiveForSomeoneElse() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "CREATE NEW OBJECTIVE FOR SOMEONE ELSE")
    } else {
      return L10n.tr_en_us("LiteralAdded", "CREATE NEW OBJECTIVE FOR SOMEONE ELSE")
    }
  }
  /// Creating Dialogs
  internal static func creatingDialogs() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Creating Dialogs")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Creating Dialogs")
    }
  }
  /// DASHBOARD
  internal static func dashboard() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "DASHBOARD")
    } else {
      return L10n.tr_en_us("LiteralAdded", "DASHBOARD")
    }
  }
  /// DATE
  internal static func date() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "DATE")
    } else {
      return L10n.tr_en_us("LiteralAdded", "DATE")
    }
  }
  /// DELIVERIES TODAY
  internal static func deliveriesToday() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "DELIVERIES TODAY")
    } else {
      return L10n.tr_en_us("LiteralAdded", "DELIVERIES TODAY")
    }
  }
  /// Delivery Date
  internal static func deliveryDate() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Delivery Date")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Delivery Date")
    }
  }
  /// DELIVERY TRIP STATUS
  internal static func deliveryTripStatus() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "DELIVERY TRIP STATUS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "DELIVERY TRIP STATUS")
    }
  }
  /// Demo Equipment
  internal static func demoEquipment() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Demo Equipment")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Demo Equipment")
    }
  }
  /// DISTRIBUTOR
  internal static func distributor() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "DISTRIBUTOR")
    } else {
      return L10n.tr_en_us("LiteralAdded", "DISTRIBUTOR")
    }
  }
  /// Ending Inventory
  internal static func endingInventory() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Ending Inventory")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Ending Inventory")
    }
  }
  /// Equipment being added
  internal static func equipmentBeingAdded() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Equipment being added")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Equipment being added")
    }
  }
  /// FAILED
  internal static func failed() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "FAILED")
    } else {
      return L10n.tr_en_us("LiteralAdded", "FAILED")
    }
  }
  /// Fault
  internal static func fault() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Fault")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Fault")
    }
  }
  /// FREE
  internal static func free() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "FREE")
    } else {
      return L10n.tr_en_us("LiteralAdded", "FREE")
    }
  }
  /// FREE REASONS
  internal static func freeReasons() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "FREE REASONS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "FREE REASONS")
    }
  }
  /// Fridge Upright
  internal static func fridgeUpright() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Fridge Upright")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Fridge Upright")
    }
  }
  /// Getting User Failed
  internal static func gettingUserFailed() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Getting User Failed")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Getting User Failed")
    }
  }
  /// Google api failed.
  internal static func googleApiFailed() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Google api failed.")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Google api failed.")
    }
  }
  /// Google api response error.
  internal static func googleApiResponseError() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Google api response error.")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Google api response error.")
    }
  }
  /// Gross Margin
  internal static func grossMargin() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Gross Margin")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Gross Margin")
    }
  }
  /// GROUPS
  internal static func groups() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "GROUPS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "GROUPS")
    }
  }
  /// Hi
  internal static func hi() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Hi")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Hi")
    }
  }
  /// In Stock and Available
  internal static func inStockAndAvailable() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "In Stock and Available")
    } else {
      return L10n.tr_en_us("LiteralAdded", "In Stock and Available")
    }
  }
  /// Invalid Sign On
  internal static func invalidSignOn() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Invalid Sign On")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Invalid Sign On")
    }
  }
  /// Inventory
  internal static func inventory() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Inventory")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Inventory")
    }
  }
  /// LAST ORDERS
  internal static func lastOrders() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "LAST ORDERS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "LAST ORDERS")
    }
  }
  /// Line
  internal static func line() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Line")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Line")
    }
  }
  /// LOG IN
  internal static func logIn() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "LOG IN")
    } else {
      return L10n.tr_en_us("LiteralAdded", "LOG IN")
    }
  }
  /// MTD
  internal static func mtd() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "MTD")
    } else {
      return L10n.tr_en_us("LiteralAdded", "MTD")
    }
  }
  /// Name *
  internal static func name() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Name *")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Name *")
    }
  }
  /// NEW ASSET FOUND
  internal static func newAssetFound() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "NEW ASSET FOUND")
    } else {
      return L10n.tr_en_us("LiteralAdded", "NEW ASSET FOUND")
    }
  }
  /// No Customer Selected
  internal static func noCustomerSelected() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "No Customer Selected")
    } else {
      return L10n.tr_en_us("LiteralAdded", "No Customer Selected")
    }
  }
  /// No Sale Reason
  internal static func noSaleReason() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "No Sale Reason")
    } else {
      return L10n.tr_en_us("LiteralAdded", "No Sale Reason")
    }
  }
  /// No Visit Reasons
  internal static func noVisitReasons() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "No Visit Reasons")
    } else {
      return L10n.tr_en_us("LiteralAdded", "No Visit Reasons")
    }
  }
  /// Not available
  internal static func notAvailable() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Not available")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Not available")
    }
  }
  /// Notes
  internal static func notes() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Notes")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Notes")
    }
  }
  /// ORDER ADJUSTMENTS
  internal static func orderAdjustments() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "ORDER ADJUSTMENTS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "ORDER ADJUSTMENTS")
    }
  }
  /// ORDER REVIEW
  internal static func orderReview() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "ORDER REVIEW")
    } else {
      return L10n.tr_en_us("LiteralAdded", "ORDER REVIEW")
    }
  }
  /// Orders
  internal static func orders() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Orders")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Orders")
    }
  }
  /// Please enter search key
  internal static func pleaseEnterSearchKey() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Please enter search key")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Please enter search key")
    }
  }
  /// Please select a product
  internal static func pleaseSelectAProduct() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Please select a product")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Please select a product")
    }
  }
  /// Please select Equipment Model
  internal static func pleaseSelectEquipmentModel() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Please select Equipment Model")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Please select Equipment Model")
    }
  }
  /// PO reference number
  internal static func poReferenceNumber() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "PO reference number")
    } else {
      return L10n.tr_en_us("LiteralAdded", "PO reference number")
    }
  }
  /// Post Trip Inspection
  internal static func postTripInspection() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Post Trip Inspection")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Post Trip Inspection")
    }
  }
  /// Prior Balance
  internal static func priorBalance() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Prior Balance")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Prior Balance")
    }
  }
  /// PRODUCT DETAILS
  internal static func productDetails() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "PRODUCT DETAILS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "PRODUCT DETAILS")
    }
  }
  /// Push notifications unsubscribe failed
  internal static func pushNotificationsUnsubscribeFailed() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Push notifications unsubscribe failed")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Push notifications unsubscribe failed")
    }
  }
  /// Repair Contact Details
  internal static func repairContactDetails() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Repair Contact Details")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Repair Contact Details")
    }
  }
  /// REPAIR EQUIPMENT
  internal static func repairEquipment() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "REPAIR EQUIPMENT")
    } else {
      return L10n.tr_en_us("LiteralAdded", "REPAIR EQUIPMENT")
    }
  }
  /// REQUEST NEW PLACEMENT
  internal static func requestNewPlacement() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "REQUEST NEW PLACEMENT")
    } else {
      return L10n.tr_en_us("LiteralAdded", "REQUEST NEW PLACEMENT")
    }
  }
  /// REQUEST PLACEMENT
  internal static func requestPlacement() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "REQUEST PLACEMENT")
    } else {
      return L10n.tr_en_us("LiteralAdded", "REQUEST PLACEMENT")
    }
  }
  /// RETURN
  internal static func `return`() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "RETURN")
    } else {
      return L10n.tr_en_us("LiteralAdded", "RETURN")
    }
  }
  /// RETURN REASONS
  internal static func returnReasons() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "RETURN REASONS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "RETURN REASONS")
    }
  }
  /// RETURN TO ORDER
  internal static func returnToOrder() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "RETURN TO ORDER")
    } else {
      return L10n.tr_en_us("LiteralAdded", "RETURN TO ORDER")
    }
  }
  /// Returns
  internal static func returns() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Returns")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Returns")
    }
  }
  /// RETURNS
  internal static func returns() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "RETURNS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "RETURNS")
    }
  }
  /// SALES
  internal static func sales() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "SALES")
    } else {
      return L10n.tr_en_us("LiteralAdded", "SALES")
    }
  }
  /// Sales Performance
  internal static func salesPerformance() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Sales Performance")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Sales Performance")
    }
  }
  /// SELECT A VISIT REASON
  internal static func selectAVisitReason() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "SELECT A VISIT REASON")
    } else {
      return L10n.tr_en_us("LiteralAdded", "SELECT A VISIT REASON")
    }
  }
  /// Select All
  internal static func selectAll() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Select All")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Select All")
    }
  }
  /// SELECT PRINTER
  internal static func selectPrinter() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "SELECT PRINTER")
    } else {
      return L10n.tr_en_us("LiteralAdded", "SELECT PRINTER")
    }
  }
  /// SELECT STORE TYPE
  internal static func selectStoreType() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "SELECT STORE TYPE")
    } else {
      return L10n.tr_en_us("LiteralAdded", "SELECT STORE TYPE")
    }
  }
  /// SHOW MARGIN
  internal static func showMargin() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "SHOW MARGIN")
    } else {
      return L10n.tr_en_us("LiteralAdded", "SHOW MARGIN")
    }
  }
  /// Signature
  internal static func signature() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Signature")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Signature")
    }
  }
  /// Signature *
  internal static func signature() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Signature *")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Signature *")
    }
  }
  /// Successfuly unsubscribed from push notifications
  internal static func successfulyUnsubscribedFromPushNotifications() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Successfuly unsubscribed from push notifications")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Successfuly unsubscribed from push notifications")
    }
  }
  /// SUPERMARKETS
  internal static func supermarkets() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "SUPERMARKETS")
    } else {
      return L10n.tr_en_us("LiteralAdded", "SUPERMARKETS")
    }
  }
  /// Tax
  internal static func tax() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Tax")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Tax")
    }
  }
  /// Terms
  internal static func terms() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Terms")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Terms")
    }
  }
  /// This Order
  internal static func thisOrder() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "This Order")
    } else {
      return L10n.tr_en_us("LiteralAdded", "This Order")
    }
  }
  /// Total
  internal static func total() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Total")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Total")
    }
  }
  /// Total to Collect
  internal static func totalToCollect() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Total to Collect")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Total to Collect")
    }
  }
  /// Trip
  internal static func trip() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Trip")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Trip")
    }
  }
  /// Update Order
  internal static func updateOrder() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "Update Order")
    } else {
      return L10n.tr_en_us("LiteralAdded", "Update Order")
    }
  }
  /// USERNAME
  internal static func username() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "USERNAME")
    } else {
      return L10n.tr_en_us("LiteralAdded", "USERNAME")
    }
  }
  /// VIEW VEHICLE STOCK
  internal static func viewVehicleStock() -> String {
    if (GlobalInfo.language == .en_gb) {
      return L10n.tr("LiteralAdded", "VIEW VEHICLE STOCK")
    } else {
      return L10n.tr_en_us("LiteralAdded", "VIEW VEHICLE STOCK")
    }
  }

  internal static func failedInAddingSomePhotos -> String {
      if (GlobalInfo.language == .en_gb) {
        return L10n.tr("LiteralAdded", "Failed in adding some photos.\nPlease Toggle 'iCloud Photo Sharing' switch in iOS Setting -> Accounts & Passwords -> iCloud -> Photos and try again.")
      } else {
        return L10n.tr_en_us("LiteralAdded", "Failed in adding some photos.\nPlease Toggle 'iCloud Photo Sharing' switch in iOS Setting -> Accounts & Passwords -> iCloud -> Photos and try again.")
      }
  }

    internal static func noPrinterLabel() -> String {
      if (GlobalInfo.language == .en_gb) {
        return L10n.tr("LiteralAdded", "VIEW VEHICLE STOCK")
      } else {
        return L10n.tr_en_us("LiteralAdded", "VIEW VEHICLE STOCK")
      }
    }

